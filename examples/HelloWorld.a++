; this is a comment test
; implementing comments is relatively similar to strings

message = "Hello World!\n\0"

; i wonder if this will work

proc main
{
    rax = SYS_WRITE
    rdi = STDOUT
    rsi = message
    rdx = len(message)
    syscall

    rax = SYS_EXIT
    rdi = 0x00
    syscall
}
